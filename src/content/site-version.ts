/**
 * Bump when you ship a broad content or nav change so sitemap `lastModified` for static
 * routes stays honest. (Dynamic routes use `publishedDate` from content modules.)
 */
export const marketingSiteLastModified = new Date("2026-04-30");
